{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"sc/","title":"Simulation and Control","text":"<p>This section covers simulation environments and robot control pipelines.</p>"},{"location":"sc/#contents","title":"Contents","text":""},{"location":"sc/#1-ar4-robot-modeling","title":"1. AR4 Robot Modeling","text":"<ul> <li>URDF Fundamentals</li> <li>Getting URDF Models</li> <li>Robot Description transfer: .Xarco to .URDF</li> </ul>"},{"location":"sc/#2-simulation-setup","title":"2. Simulation Setup","text":"<ul> <li>USD Import in Isaac Sim</li> <li>Isaac Sim Robot Configuration</li> </ul>"},{"location":"sc/#3-simulation-and-control-pipline","title":"3. Simulation and Control Pipline","text":"<ul> <li>AR4 Gazebo + MoveIt Control</li> <li>AR4 Target Follow in IsaacSim</li> <li>AR4 Gripper Control in IsaacSim</li> <li>AR4 Pick and Place in IsaacSim</li> <li>Controlling Isaac Sim from ROS2</li> </ul>"},{"location":"sc/AR4_Gazebo_MoveIt/","title":"AR4 Robot Control with Gazebo and MoveIt (ROS2)","text":""},{"location":"sc/AR4_Gazebo_MoveIt/#pipeline-overview","title":"Pipeline Overview","text":"<p>This tutorial demonstrates how to control the AR4 robot using MoveIt in RViz while running physics simulation in Gazebo. </p>"},{"location":"sc/AR4_Gazebo_MoveIt/#setup-workspace","title":"Setup Workspace","text":"<ol> <li> <p>Using ROS2 Humble on Ubuntu 22.04, create workspace and clone repository:</p> <pre><code>mkdir -p ~/ar4_ws/src\ncd ~/ar4_ws/src \ngit clone -b humble https://github.com/ycheng517/ar4_ros_driver.git\ncd ..\n</code></pre> </li> <li> <p>Install dependencies:</p> <pre><code>sudo apt install python3-rosdep\nsudo rosdep init\nrosdep update\nrosdep install --from-paths . --ignore-src -r -y\n</code></pre> </li> <li> <p>Build workspace:</p> <pre><code>cd ~/ar4_ws\ncolcon build\nsource install/setup.bash\n</code></pre> </li> <li> <p>Verify Installation, when you run below, you will see the figure:</p> <p><pre><code>ros2 pkg list | grep annin_ar4\n</code></pre> </p> </li> </ol>"},{"location":"sc/AR4_Gazebo_MoveIt/#control-simulated-arm-in-gazebo-with-moveit-in-rviz","title":"Control simulated arm in Gazebo with MoveIt in RViz","text":"<ol> <li> <p>Launch Gazebo Simulation</p> <pre><code>ros2 launch annin_ar4_gazebo gazebo.launch.py\n</code></pre> </li> <li> <p>Launch MoveIt in RViz</p> <p>Open a new terminal:</p> <pre><code>cd ~/ar4_ws\nsource install/setup.bash\nros2 launch annin_ar4_moveit_config moveit.launch.py use_sim_time:=true include_gripper:=True\n</code></pre> <p>You will see two scenes, one is gazebo (physics simulation), another is rviz (motion planning):</p> <p></p> </li> </ol>"},{"location":"sc/AR4_Gazebo_MoveIt/#control-the-robot-in-rviz","title":"Control the Robot in RViz","text":"<ol> <li> <p>In rviz graphic interface, use the interactive marker to move the end-effector and click Plan &amp; Execute :</p> <p></p> </li> <li> <p>Here is the video demo, you will see the simulation in gazebo followed your control in the rviz.</p> <p><p> </p></p> </li> </ol>"},{"location":"sc/AR4_Target_follow/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"sc/AR4_Target_follow/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"sc/AR4_Target_follow/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"sc/AR4_Target_follow/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"sc/AR4_Target_follow/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"sc/AR4_Target_follow/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"sc/AR4_Target_follow/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"sc/AR4_gripper_control/","title":"AR4 Gripper Control in IsaacSim","text":"<p>Load the AR4 MK3 USD model in Isaac Sim with Python, and drive its parallel gripper through a smooth sinusoidal open-close motion using <code>ParallelGripper</code>.</p> <p> </p>"},{"location":"sc/AR4_gripper_control/#0-what-this-tutorial-covers","title":"0. What this tutorial covers","text":"<p>By the end of this walkthrough you will be able to:</p> <ol> <li>Load the AR4 MK3 USD into an Isaac Sim from a Python script;</li> <li>Auto locate the end-effector (<code>gripper_base_link</code>) instead of hard coding a long prim path;</li> <li>Drive the gripper with <code>ParallelGripper</code> and a USD <code>mimic</code> joint so both jaws move together;</li> <li>Animate the jaws with a sine wave so you can visually verify the control loop is working.</li> </ol>"},{"location":"sc/AR4_gripper_control/#1-prerequisites","title":"1. Prerequisites","text":"<ul> <li>Isaac Sim 5.1</li> <li>Python 3.10 (the interpreter shipped with Isaac Sim)</li> <li>An AR4 MK3 USD file. In this tutorial the path is:</li> </ul> <pre><code>F:/isaac/ar4_urdf/ar4_mk3_isaac/ar4_mk3_isaac.usd\n</code></pre> <p>If you only have a URDF, convert it with Isaac Sim's built-in URDF Importer. Tick <code>Fix Base Link</code> during import \u2014 otherwise the arm will spawn lying flat on the ground.</p>"},{"location":"sc/AR4_gripper_control/#2-the-big-picture","title":"2. The big picture","text":"<p>Controlling the gripper boils down to two problems:</p> <ul> <li>Find the end-effector prim. <code>ParallelGripper</code> needs an <code>end_effector_prim_path</code>, and that path has to actually exist on the USD stage. URDF-imported paths tend to be deeply nested and version-dependent, so hard-coding them is brittle. We'll discover the prim at runtime instead.</li> <li>Drive the gripper joint. AR4's gripper has two parts \u2014 one active joint (<code>gripper_jaw1_joint</code>) plus a mimic joint where jaw2 follows jaw1. Setting <code>use_mimic_joints=True</code> on <code>ParallelGripper</code> is enough to handle the coupling.</li> </ul> <p>Once those are sorted, the loop just needs to feed jaw1 a target position oscillating between <code>0</code> and <code>0.014</code> m.</p>"},{"location":"sc/AR4_gripper_control/#3-walking-through-the-code","title":"3. Walking through the code","text":""},{"location":"sc/AR4_gripper_control/#31-boot-simulationapp","title":"3.1 Boot SimulationApp","text":"<pre><code>from isaacsim import SimulationApp\nsimulation_app = SimulationApp({\"headless\": False})\n</code></pre> <p>This line must come before any other <code>isaacsim.*</code> import. Isaac Sim is built on Kit, and most modules can only be loaded after <code>SimulationApp</code> is up. Get the order wrong and you'll see cryptic import errors.</p> <p><code>headless=False</code> opens the GUI \u2014 keep it on while debugging, flip it to <code>True</code> for training runs.</p>"},{"location":"sc/AR4_gripper_control/#32-imports-and-cli-args","title":"3.2 Imports and CLI args","text":"<pre><code>import argparse\nimport numpy as np\nfrom isaacsim.core.api import World\nfrom isaacsim.core.utils.stage import add_reference_to_stage\nfrom isaacsim.core.utils.types import ArticulationAction\nfrom isaacsim.robot.manipulators import SingleManipulator\nfrom isaacsim.robot.manipulators.grippers import ParallelGripper\nimport omni.usd\n\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--test\", default=False, action=\"store_true\")\nargs, _ = parser.parse_known_args()\n\nUSD_PATH   = r\"F:/isaac/ar4_urdf/ar4_mk3_isaac/ar4_mk3_isaac.usd\"\nROBOT_PRIM = \"/World/ar4_mk3_isaac\"\n</code></pre> <p>The <code>--test</code> flag is for CI: it lets the script auto-exit after a few seconds instead of running forever.</p>"},{"location":"sc/AR4_gripper_control/#33-create-the-world-and-load-the-usd","title":"3.3 Create the World and load the USD","text":"<pre><code>my_world = World(stage_units_in_meters=1.0)\nadd_reference_to_stage(usd_path=USD_PATH, prim_path=ROBOT_PRIM)\n\n# Force one stage update so the prims actually materialize\nsimulation_app.update()\n</code></pre> <p>\u26a0\ufe0f <code>simulation_app.update()</code> is critical. <code>add_reference_to_stage</code> only attaches a reference; the prim tree underneath is expanded on the next stage tick. If you immediately call <code>Traverse()</code> you'll get an empty result and waste an hour debugging \u2014 it's a timing issue, not a path issue.</p>"},{"location":"sc/AR4_gripper_control/#34-auto-discover-the-end-effector-prim","title":"3.4 Auto-discover the end-effector prim","text":"<pre><code>stage = omni.usd.get_context().get_stage()\n\ndef find_prim_suffix(suffix: str):\n    hits = []\n    for prim in stage.Traverse():\n        p = prim.GetPath().pathString\n        if p.endswith(suffix):\n            hits.append(p)\n    return hits\n\nhits = find_prim_suffix(\"/gripper_base_link\")\nprint(\"=== Found gripper_base_link prims ===\")\nfor h in hits:\n    print(h)\n\nif len(hits) == 0:\n    raise RuntimeError(\"can't find gripper_base_link, USD load failed\")\n\n# When several candidates exist, prefer the one under link_6\nEE_PRIM = None\nfor h in hits:\n    if \"/link_6/\" in h:\n        EE_PRIM = h\n        break\nif EE_PRIM is None:\n    EE_PRIM = hits[0]\n\nprint(f\"=== Using EE_PRIM: {EE_PRIM} ===\")\n</code></pre> <p>Why bother? Because URDF-imported USDs produce paths like:</p> <pre><code>/World/ar4_mk3_isaac/base_link/link_1/link_2/.../link_6/gripper_base_link\n</code></pre> <p>The exact hierarchy varies between importer versions. Suffix matching is portable; hard-coded paths are not.</p> <p>The <code>link_6</code> preference handles a sneaky edge case: some USD models contain duplicate <code>gripper_base_link</code> prims (collision proxies, visual children, etc.) \u2014 the one nested under <code>link_6</code> is the actual kinematic end-effector.</p>"},{"location":"sc/AR4_gripper_control/#35-configure-the-parallelgripper","title":"3.5 Configure the ParallelGripper","text":"<pre><code>gripper = ParallelGripper(\n    end_effector_prim_path=EE_PRIM,\n    joint_prim_names=[\"gripper_jaw1_joint\"],\n    joint_opened_positions=np.array([0.014], dtype=np.float32),\n    joint_closed_positions=np.array([0.0], dtype=np.float32),\n    action_deltas=np.array([0.014], dtype=np.float32),\n    use_mimic_joints=True,\n)\n</code></pre> <p>Argument by argument:</p> Argument Meaning <code>end_effector_prim_path</code> The prim where forward kinematics terminates <code>joint_prim_names</code> The active joint name only <code>joint_opened_positions</code> Jaw1 position when fully open (meters); 14 mm for AR4 <code>joint_closed_positions</code> Jaw1 position when fully closed <code>action_deltas</code> Step size for discrete <code>open()</code> / <code>close()</code> actions <code>use_mimic_joints=True</code> Key flag: jaw2's mimic joint is driven automatically by USD's mimic API <p>If you leave <code>use_mimic_joints=False</code>, you have to command jaw1 and jaw2 yourself, mirror the sign correctly, and stay in lockstep frame by frame. Even a 0.001 m mismatch can jam the jaws. Just set it to <code>True</code>.</p>"},{"location":"sc/AR4_gripper_control/#36-wrap-it-in-a-singlemanipulator","title":"3.6 Wrap it in a SingleManipulator","text":"<pre><code>robot = my_world.scene.add(\n    SingleManipulator(\n        prim_path=ROBOT_PRIM,\n        name=\"ar4_mk3\",\n        end_effector_prim_path=EE_PRIM,\n        gripper=gripper,\n    )\n)\n\nmy_world.scene.add_default_ground_plane()\nmy_world.reset()\nmy_world.play()\n</code></pre> <p><code>SingleManipulator</code> is Isaac Sim's high-level wrapper that bundles \"arm body + end-effector + gripper\" into one object. From here on you can just call <code>robot.gripper.apply_action(...)</code>.</p> <p><code>my_world.reset()</code> must be called before <code>play()</code> \u2014 that's when joint handles, controllers, and other internal state get initialized. Skip it and you'll get an <code>articulation not initialized</code> error.</p>"},{"location":"sc/AR4_gripper_control/#37-the-sinusoidal-control-loop","title":"3.7 The sinusoidal control loop","text":"<pre><code>OPEN  = 0.014\nCLOSE = 0.0\nperiod_sec = 1.0   # smaller = faster\nfps_est    = 60.0  # estimated sim frame rate\n\nt = 0\nwhile simulation_app.is_running():\n    my_world.step(render=True)\n\n    # Auto-resume if the user clicks Stop in the GUI\n    if my_world.is_stopped():\n        my_world.play()\n\n    t += 1\n    phase = (t / (fps_est * period_sec)) * 2.0 * np.pi\n    s01 = 0.5 * (1.0 + np.sin(phase))      # remap to [0, 1]\n    target = CLOSE + s01 * (OPEN - CLOSE)  # remap to jaw position\n\n    robot.gripper.apply_action(\n        ArticulationAction(joint_positions=[target])\n    )\n\n    if args.test and t &gt; 180:\n        break\n\nsimulation_app.close()\n</code></pre> <p>Why a sine wave instead of toggling <code>open()</code> / <code>close()</code>?</p> <ul> <li>Square-wave switching makes the gripper slam to the target at max velocity. It looks robotic, excites mechanical chatter, and makes it hard to tune stiffness/damping.</li> <li>A sinusoidal target gives the PD controller a smooth trajectory it can actually track. The motion looks like breathing, and it's a much better signal for tuning gains.</li> </ul> <p><code>s01 = 0.5 * (1 + sin(x))</code> is the standard trick to remap <code>sin</code> from <code>[-1, 1]</code> into <code>[0, 1]</code>.</p>"},{"location":"sc/AR4_gripper_control/#4-running-it","title":"4. Running it","text":"<p>Save the script as <code>ar4_gripper_demo.py</code> and run it with Isaac Sim's bundled Python:</p> <p>Windows: <pre><code>\"C:\\Users\\&lt;you&gt;\\AppData\\Local\\ov\\pkg\\isaac-sim-5.1.0\\python.bat\" ar4_gripper_demo.py\n</code></pre></p> <p>Linux: <pre><code>./python.sh ar4_gripper_demo.py\n</code></pre></p> <p>If everything is wired up correctly, the GUI opens, AR4 stands on the ground plane, and the gripper opens and closes once per second.</p>"},{"location":"sc/AR4_gripper_control/#5-common-pitfalls","title":"5. Common pitfalls","text":"<p>1. <code>gripper_base_link</code> not found Double-check the USD path. On Windows, use a raw string <code>r\"...\"</code> or escape the backslashes.</p> <p>2. <code>articulation is not initialized</code> 99% of the time you forgot <code>my_world.reset()</code>, or you skipped <code>simulation_app.update()</code> after <code>add_reference_to_stage</code>.</p> <p>3. Only one finger moves The mimic joint isn't active. Two possible causes: - <code>use_mimic_joints=False</code> (the default \u2014 you have to opt in explicitly); - The mimic tag wasn't carried over during URDF \u2192 USD conversion. Open the USD file and search for <code>physics:MimicAPI</code>. If it's missing, either patch it in by hand or re-import with a newer version of the URDF importer.</p> <p>4. Frame-rate jitter throws off the period <code>fps_est = 60.0</code> is just an estimate. For an exact period, drive the phase from sim time instead of frame count:</p> <pre><code>phase = (my_world.current_time / period_sec) * 2.0 * np.pi\n</code></pre> <p>Now the cycle is exactly <code>period_sec</code> seconds regardless of actual frame rate.</p>"},{"location":"sc/AR4_gripper_control/#6-full-script","title":"6. Full script","text":"<p>Drop-in runnable. Just update <code>USD_PATH</code>.</p> <pre><code>from isaacsim import SimulationApp\nsimulation_app = SimulationApp({\"headless\": False})\n\nimport argparse\nimport numpy as np\nfrom isaacsim.core.api import World\nfrom isaacsim.core.utils.stage import add_reference_to_stage\nfrom isaacsim.core.utils.types import ArticulationAction\nfrom isaacsim.robot.manipulators import SingleManipulator\nfrom isaacsim.robot.manipulators.grippers import ParallelGripper\nimport omni.usd\n\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--test\", default=False, action=\"store_true\")\nargs, _ = parser.parse_known_args()\n\nUSD_PATH   = r\"F:/isaac/ar4_urdf/ar4_mk3_isaac/ar4_mk3_isaac.usd\"\nROBOT_PRIM = \"/World/ar4_mk3_isaac\"\n\nmy_world = World(stage_units_in_meters=1.0)\nadd_reference_to_stage(usd_path=USD_PATH, prim_path=ROBOT_PRIM)\nsimulation_app.update()\n\nstage = omni.usd.get_context().get_stage()\n\ndef find_prim_suffix(suffix: str):\n    return [p.GetPath().pathString for p in stage.Traverse()\n            if p.GetPath().pathString.endswith(suffix)]\n\nhits = find_prim_suffix(\"/gripper_base_link\")\nprint(\"=== Found gripper_base_link prims ===\")\nfor h in hits:\n    print(h)\nif not hits:\n    raise RuntimeError(\"can't find gripper_base_link, USD load failed\")\n\nEE_PRIM = next((h for h in hits if \"/link_6/\" in h), hits[0])\nprint(f\"=== Using EE_PRIM: {EE_PRIM} ===\")\n\ngripper = ParallelGripper(\n    end_effector_prim_path=EE_PRIM,\n    joint_prim_names=[\"gripper_jaw1_joint\"],\n    joint_opened_positions=np.array([0.014], dtype=np.float32),\n    joint_closed_positions=np.array([0.0], dtype=np.float32),\n    action_deltas=np.array([0.014], dtype=np.float32),\n    use_mimic_joints=True,\n)\n\nrobot = my_world.scene.add(\n    SingleManipulator(\n        prim_path=ROBOT_PRIM,\n        name=\"ar4_mk3\",\n        end_effector_prim_path=EE_PRIM,\n        gripper=gripper,\n    )\n)\n\nmy_world.scene.add_default_ground_plane()\nmy_world.reset()\nmy_world.play()\n\nOPEN, CLOSE = 0.014, 0.0\nperiod_sec, fps_est = 1.0, 60.0\n\nt = 0\nwhile simulation_app.is_running():\n    my_world.step(render=True)\n    if my_world.is_stopped():\n        my_world.play()\n\n    t += 1\n    phase = (t / (fps_est * period_sec)) * 2.0 * np.pi\n    s01 = 0.5 * (1.0 + np.sin(phase))\n    target = CLOSE + s01 * (OPEN - CLOSE)\n\n    robot.gripper.apply_action(\n        ArticulationAction(joint_positions=[target])\n    )\n\n    if args.test and t &gt; 180:\n        break\n\nsimulation_app.close()\n</code></pre>"},{"location":"sc/AR4_pick_and_place/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"sc/AR4_pick_and_place/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"sc/AR4_pick_and_place/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"sc/AR4_pick_and_place/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"sc/AR4_pick_and_place/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"sc/AR4_pick_and_place/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"sc/AR4_pick_and_place/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"sc/Cross_Platform/","title":"ROS2\u2013Isaac Sim Integration for AR4 (Linux \u2194 Windows)","text":"<p>This tutorial shows how to connect the AR4 robot running in ROS2 on Linux with Isaac Sim running on Windows. After the connection is established, you can control the robot from RViz / MoveIt on Linux and visualize the motion in Isaac Sim on Windows.</p>"},{"location":"sc/Cross_Platform/#requirements","title":"Requirements","text":"<ul> <li>ROS2 Humble installed on Linux 22.04</li> <li>AR4 ROS2 workspace already built on Linux 22.04</li> <li>Isaac Sim 5.1 installed on Windows</li> <li>AR4 robot model already imported into Isaac Sim</li> <li>ROS2 Bridge extension available in Isaac Sim </li> </ul>"},{"location":"sc/Cross_Platform/#set-ros-domain-and-dds-configuration","title":"Set ROS domain and DDS configuration","text":"<ol> <li> <p>To let Linux ROS2 and Isaac Sim communicate for both Linux and windows machines, make sure both sides use the same:</p> <ul> <li><code>ROS_DOMAIN_ID</code></li> <li><code>RMW_IMPLEMENTATION</code></li> <li><code>ROS_LOCALHOST_ONLY</code></li> </ul> </li> <li> <p>On Linux:</p> <p>Open a terminal and run:</p> <pre><code>cd ar4_ws\nsource ~/ar4_ws/install/setup.bash\nexport ROS_DOMAIN_ID=42\nexport RMW_IMPLEMENTATION=rmw_fastrtps_cpp\nexport ROS_LOCALHOST_ONLY=0\nros2 daemon stop\nros2 daemon start\n</code></pre> </li> <li> <p>On Windows:</p> <p>Navigate to Isaac Sim installation folder and open a terminal there.  </p> <p><pre><code>$env:ROS_DOMAIN_ID=\"42\"\n$env:RMW_IMPLEMENTATION=\"rmw_fastrtps_cpp\"\n$env:ROS_LOCALHOST_ONLY=\"0\"\n</code></pre>   To verify, run:</p> <p><pre><code>echo $env:ROS_DOMAIN_ID\necho $env:RMW_IMPLEMENTATION\necho $env:ROS_LOCALHOST_ONLY\n</code></pre>   You will see on both Linux and Windows terminals:</p> <ul> <li>42</li> <li><code>rmw_fastrips_cpp</code></li> <li><code>0</code> </li> </ul> <p>If so, the basic ROS2 communication is configured correctly.   (PS: Do not close these terminals after setting the environment variables.)</p> </li> </ol>"},{"location":"sc/Cross_Platform/#connection-verification","title":"Connection Verification","text":"<ol> <li> <p>Start Isaac Sim with ROS2 Bridge</p> <p>In the Isaac Sim installation directory on Windows, run:</p> <pre><code>.\\isaac-sim.bat --/isaac/startup/ros_bridge_extension=isaacsim.ros2.bridge\n</code></pre> <p>This launches Isaac Sim with the ROS2 Bridge extension enabled.</p> </li> <li> <p>Verify ROS2 connection inside Isaac Sim</p> <p>In Isaac Sim:</p> <ul> <li> <p>Click <code>Window</code> \u2192  <code>Graph Editor</code></p> </li> <li> <p>Create a new <code>Action Graph</code></p> </li> </ul> <p>We will build a simple clock publisher graph to verify the connection.</p> <p>When you open the action graph, add the following nodes:</p> <ul> <li> <p><code>On Playback Tick</code></p> </li> <li> <p><code>ROS2 Context</code></p> </li> <li> <p><code>Isaac Read Simulation Time</code></p> </li> <li> <p><code>ROS2 Publish Clock</code></p> </li> </ul> <p>Connect them as shown in figure below.</p> <p></p> </li> <li> <p>Check ROS2 topics on Linux</p> <p>Go back to Linux and run in terminal:</p> <p><pre><code>ros2 node list\nros2 topic list\n</code></pre>   If <code>/clock</code> appears, ROS2 communication between Linux and Isaac Sim is working.</p> </li> </ol>"},{"location":"sc/Cross_Platform/#joint-state-communication","title":"Joint State Communication","text":"<ol> <li> <p>Publish joint states from Isaac Sim</p> <p>Next, we publish the AR4 robot joint states from Isaac Sim to ROS2.</p> <p>Create a new action graph and add\uff1a</p> <ul> <li> <p><code>On Playback Tick</code></p> </li> <li> <p><code>Articulation State</code></p> </li> <li> <p><code>Isaac Read Simulation Time</code></p> </li> <li> <p><code>Ros2 Publish Joint State</code></p> </li> </ul> <p>Connect them as shown in Figure below.</p> <p></p> </li> <li> <p>Configure node properties</p> <p>For the <code>Articulation State</code> and <code>ROS2 Publish Joint State</code> nodes in action graph:</p> <ul> <li>Set <code>targetPrim</code> to the AR4 articulation root:</li> </ul> <pre><code>/World/mk3/base_link\n</code></pre> <ul> <li>Fill <code>jointNames</code> as:</li> </ul> <pre><code>joint_1\njoint_2\njoint_3\njoint_4\njoint_5\njoint_6\n</code></pre> <p>After fill out these , return to terminal in Linux and run:</p> <pre><code>ros2 topic list\n</code></pre> <p>You will see the figure below, demonstrating you communicate successfully.</p> <p></p> </li> </ol>"},{"location":"sc/Cross_Platform/#motion-command-pipeline","title":"Motion Command Pipeline","text":"<ol> <li> <p>Prepare trajectory format conversion</p> <p>Before subscribing to MoveIt output and sending commands into Isaac Sim, we need to preprocess the trajectory message format.</p> <p>Create a new folder under ar4_ws, for example:</p> <pre><code>cd ar4_ws\nmkdir -p tools\ncd tools\n</code></pre> <p>Create a Python script, download the script below and put it into <code>tools</code> folder. This script converts MoveIt display trajectory messages into a format that Isaac Sim can subscribe to.</p> <p>relay_display_traj_to_jointstate.py</p> </li> <li> <p>Create the subscriber and articulation controller graph</p> <p>Now we create another Action Graph in Isaac Sim for robot control.</p> <p>Add:</p> <ul> <li> <p><code>On Playback Tick</code></p> </li> <li> <p><code>ROS2 Subscriber</code></p> </li> <li> <p><code>Articulation Controller</code></p> </li> </ul> <p>Connect them as shown in figure below.</p> <p></p> <p>Configure the ROS2 Subscriber node:</p> <p>Set:</p> <p><pre><code>messagePackage = sensor_msgs\nmessageSubfolder = msg\nmessageName = JointState\ntopicName = /isaac_joint_targets\n</code></pre>   Configure the Articulation Controller node:</p> <p>Set targetPrim to:</p> <p><pre><code>/World/mk3/base_link\n</code></pre> 3. Run the relay script on Linux</p> <p>Back on Linux, go to the tools folder and run in the new terminal base on this folder:</p> <p><pre><code>cd tools\nchmod +x relay_display_traj_to_jointstate.py\n./relay_display_traj_to_jointstate.py\n</code></pre> 4. Launch MoveIt demo on Linux</p> <p>Open a new terminal on Linux:</p> <pre><code>cd ar4_ws\nsource install/setup.bash\nros2 launch annin_ar4_moveit_config demo.launch.py ar_model:=mk3\n</code></pre> </li> </ol> <p>At this stage:</p> <ul> <li>Isaac Sim should already be open on Windows</li> <li>The Action Graph should be running</li> <li>The relay script should already be running on Linux</li> <li>RViz / MoveIt should already be open on Linux</li> </ul> <p>Now:</p> <ul> <li>Click <code>Play</code> in Isaac Sim</li> <li>In RViz, drag the interactive marker, click <code>Plan &amp; Execute</code> the motion.</li> </ul> <p>You will see the communication, shown as video below:</p> <p> </p>"},{"location":"sc/Cross_Platform/#run-the-final-control-loop","title":"Run the final control loop","text":""},{"location":"sc/Getting_URDF_Models/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"sc/Getting_URDF_Models/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"sc/Getting_URDF_Models/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"sc/Getting_URDF_Models/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"sc/Getting_URDF_Models/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"sc/Getting_URDF_Models/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"sc/Getting_URDF_Models/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"sc/Isaac_Sim_Robot_Configuration/","title":"Isaac Sim Robot Configuration","text":""},{"location":"sc/Isaac_Sim_Robot_Configuration/#introduction","title":"Introduction","text":"<p>After importing the AR4 into Isaac Sim, the robot needs a configuration file before simulation. These configuration files describe the kinematic chain, joint limits, and collision geometry in a form the solvers understand.</p> <p>This page goes through generating those files for the AR4 using the Lula Robot Description Editor built into Isaac Sim. </p>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#prerequisites","title":"Prerequisites","text":"<ol> <li> <p>The AR4 <code>.usd</code> file has been imported into Isaac Sim. See USD Import in Isaac Sim if you have not done this yet.</p> </li> <li> <p>You have the AR4 URDF file on disk.</p> </li> <li> <p>Articulation root path: /World/ar4_mk3_isaac.</p> </li> </ol>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#enable-the-lula-extension","title":"Enable the Lula Extension","text":"<ol> <li> <p>Go to Window \u2192 Extensions.</p> </li> <li> <p>Type <code>Lula</code> in the search box and find Isaac Sim Lula.</p> </li> <li> <p>If it does not appear, remove the <code>@feature</code> filter.</p> </li> <li> <p>Click ENABLE, then check AUTOLOAD so it persists across sessions.</p> </li> </ol>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#prepare-the-ar4-asset-for-lula","title":"Prepare the AR4 Asset for Lula","text":"<p>The Lula Robot Description Editor does not support instantiable meshes, so before opening the editor we need to flatten them.</p> <ol> <li> <p>Open the AR4 USD file in Isaac Sim.</p> </li> <li> <p>In the Stage panel, search for <code>visuals</code> and select all matching prims.</p> </li> <li> <p>In the Property panel, uncheck Instantiable.</p> </li> <li> <p>Repeat for all <code>collisions</code> prims.</p> </li> </ol>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#configure-joints-in-the-lula-robot-description-editor","title":"Configure Joints in the Lula Robot Description Editor","text":"<ol> <li> <p>Press PLAY to start the simulation. Keep the simulation running for the rest of this page. Stopping it will reset the editor and you'll lose your   work.</p> </li> <li> <p>Open Tools \u2192 Lula Robot Description Editor.</p> </li> <li> <p>In the Selection Panel, select the AR4 articulation (<code>/World/ar4_mk3_isaac</code>).</p> </li> <li> <p>Scroll to the Set Joint Properties section.</p> </li> <li> <p>For each joint, set Joint Status as follows:</p> Joint Joint Status Notes <code>joint_1</code> \u2026 <code>joint_6</code> Active Joint The 6 revolute arm joints driven by the motion solver. Gripper joint(s) Fixed Joint Gripper is controlled separately, so Lula treats it as rigid during planning. <ul> <li> <p>Leave the other settings (Joint Position, Acceleration Limit, Jerk Limit) at their defaults unless you have a specific reason to change them. </p> </li> <li> <p>Here is Joint Properties panel. All six AR4 arm joints are set to Active Joint with default acceleration and jerk limits.</p> <p></p> </li> </ul> </li> </ol>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#generate-collision-spheres","title":"Generate Collision Spheres","text":"<ol> <li> <p>Scroll to the Link Sphere Editor section.</p> </li> <li> <p>In Selection Panel \u2192 Select link, pick a link (start with <code>base_link</code>, then move down the chain).</p> </li> <li> <p>In Generate Spheres \u2192 Select Mesh, choose their collision mesh, e.g. <code>/collisions/base_link/mesh</code>.</p> </li> <li> <p>Set Radius Offset to <code>0.03</code>. This pads the sphere radius slightly beyond the mesh to give the solver a safety margin.</p> </li> <li> <p>Set Number of Spheres to <code>8</code>.</p> </li> <li> <p>Confirm the spheres appear in red on the link in the viewport.</p> </li> <li> <p>Click Generate Spheres. The spheres turn yellow to confirm they're committed.</p> </li> <li> <p>Repeat for all six AR4 arm links plus any gripper links you want considered for collision.</p> </li> </ol> <p></p>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#export-the-configuration-files","title":"Export the Configuration Files","text":"<ol> <li> <p>Export the Lula Robot Description (YAML)</p> <ul> <li> <p>Scroll to the Export To File section at the bottom of the editor.</p> </li> <li> <p>Expand Export to Lula Robot Description File.</p> </li> <li> <p>Click the file icon and save as <code>ar4.yaml</code> somewhere your project can find it.</p> </li> <li> <p>Click Save.</p> </li> </ul> <p></p> </li> <li> <p>Export the cuMotion XRDF.</p> <ul> <li> <p>In the same Export To File section, expand Export to cuMotion XRDF.</p> </li> <li> <p>Save as <code>ar4.xrdf</code>.</p> </li> </ul> <p></p> </li> <li> <p>Stop the simulation</p> </li> </ol>"},{"location":"sc/Isaac_Sim_Robot_Configuration/#what-you-now-have","title":"What You Now Have","text":"<p>After this page you should have:</p> <pre><code>```\n\nar4_workspace/\n   \u251c\u2500\u2500 ar4.urdf              # from the URDF transfer step\n   \u251c\u2500\u2500 ar4.usd               # from the USD import step\n   \u251c\u2500\u2500 ar4.yaml              # Lula Robot Description \n   \u2514\u2500\u2500 ar4.xrdf              # cuMotion XRDF\n</code></pre> <p>```</p>"},{"location":"sc/URDF_Fundamentals/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"sc/URDF_Fundamentals/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"sc/URDF_Fundamentals/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"sc/URDF_Fundamentals/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"sc/URDF_Fundamentals/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"sc/URDF_Fundamentals/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"sc/URDF_Fundamentals/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"sc/USD_Import_in_Isaac_Sim/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"sc/USD_Import_in_Isaac_Sim/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"sc/USD_Import_in_Isaac_Sim/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"sc/USD_Import_in_Isaac_Sim/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"sc/USD_Import_in_Isaac_Sim/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"sc/USD_Import_in_Isaac_Sim/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"sc/USD_Import_in_Isaac_Sim/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"sc/Xacro_to_URDF/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"sc/Xacro_to_URDF/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"sc/Xacro_to_URDF/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"sc/Xacro_to_URDF/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"sc/Xacro_to_URDF/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"sc/Xacro_to_URDF/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"sc/Xacro_to_URDF/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"setup/","title":"Setup","text":"<p>This section covers installation and environment preparation.</p>"},{"location":"setup/#contents","title":"Contents","text":""},{"location":"setup/#1-installation","title":"1. Installation","text":"<ul> <li>Windows Installation Guide: Isaac Sim</li> <li>Windows Installation Guide: Isaac Lab</li> <li>ROS2 Humble Installation on Linux Guide</li> </ul>"},{"location":"setup/#2-environment","title":"2. Environment","text":"<ul> <li>Virtual Machine Running Linux</li> <li>Windows Subsystem for Linux (WSL)</li> </ul>"},{"location":"setup/windows_isaac_lab/","title":"Windows Installation Guide: Isaac Lab","text":""},{"location":"setup/windows_isaac_lab/#what-is-isaac-lab","title":"What is Isaac Lab?","text":"<ol> <li>Please see here: </li> </ol> <p>Isaac Lab Overview</p>"},{"location":"setup/windows_isaac_lab/#create-a-conda-environment","title":"Create a Conda Environment","text":"<ol> <li> <p>Before we install,  we need a virtual environment. The official documentation recommends Miniconda, but installing full Anaconda is also acceptable.</p> <p>Download Link: Anaconda</p> </li> <li> <p>After installation, open Anaconda Navigation interface, select anaconda_prompt, the UI is shown below:</p> <p></p> </li> <li> <p>Open the terminal, and run</p> <pre><code>conda create -n env_isaaclab python=3.11\nconda activate env_isaaclab\n</code></pre> </li> <li> <p>Ensure the latest pip version is installed. To update pip, run the following command from inside the virtual environment:</p> <pre><code>python -m pip install --upgrade pip\n</code></pre> </li> </ol>"},{"location":"setup/windows_isaac_lab/#install-dependencies","title":"Install Dependencies","text":"<ol> <li> <p>Install Isaac Sim pip packages, run:</p> <pre><code>pip install \"isaacsim[all,extscache]==5.1.0\" --extra-index-url https://pypi.nvidia.com\n</code></pre> </li> <li> <p>Install a CUDA-enabled PyTorch build, run:</p> <pre><code>pip install -U torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128\n</code></pre> </li> <li> <p>Check that the simulator runs as expected, run:</p> <p><pre><code>Isaacsim\n</code></pre> The successful installation is showing the Isaac Sim UI.</p> </li> </ol>"},{"location":"setup/windows_isaac_lab/#install-isaac-lab","title":"Install Isaac Lab","text":"<ol> <li> <p>Clone the Isaac Lab repository into your project\u2019s workspace:</p> <p><pre><code>git clone https://github.com/isaac-sim/IsaacLab.git\n</code></pre>    Import Note:</p> <p>If you want to clone to another drive , open a separate PowerShell window, switch to that drive first, and input in Terminal, for example:</p> <p><pre><code>F:\n</code></pre>    Then navigate to the cloned folder, for example:</p> <pre><code>cd isaac\\IsaacLab\n</code></pre> </li> <li> <p>Install Isaac Lab, run:</p> <pre><code>isaaclab.bat --install :: or \"isaaclab.bat -i\n</code></pre> </li> </ol>"},{"location":"setup/windows_isaac_lab/#verifying-the-isaac-lab-installation","title":"Verifying the Isaac Lab installation","text":"<ol> <li> <p>Create Empty in Isaac Sim, run\uff1a</p> <p><pre><code>isaaclab.bat -p scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre>    or </p> <pre><code>python scripts\\tutorials\\00_sim\\create_empty.py\n</code></pre> <p>After running, if you see the figure below, the Isaac Lab is successfully installed:</p> <p></p> </li> <li> <p>Train a Robot</p> <ul> <li>Install all RL dependencies:</li> </ul> <p><pre><code>isaaclab.bat -i all\n</code></pre>    - Train the Ant Robot</p> <p><pre><code>isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \u2013headless\n</code></pre>    The learning iteration information is shown below:</p> <p></p> <ul> <li>If you wanna see GUI, run:<pre><code>  ``` \n  isaaclab.bat -p scripts/reinforcement_learning/rsl_rl/train.py --task=Isaac-Ant-v0 \n   ```\n</code></pre> </li> </ul> <p></p> </li> </ol>"},{"location":"setup/windows_isaac_lab/#close-isaac-lab-running","title":"Close Isaac Lab Running","text":"<p>Unlike the standard way of exiting a terminal with Ctrl+C, Isaac Lab must be closed using Ctrl + Fn + Pause.</p>"},{"location":"setup/windows_isaac_sim/","title":"Windows Installation Guide: Isaac Sim","text":""},{"location":"setup/windows_isaac_sim/#check-official-documentation","title":"Check Official Documentation","text":"<ol> <li> <p>Please refer to the official NVIDIA documentation:    Isaac Sim Installation Guide</p> </li> <li> <p>At the top of the page, switch the version to 5.1.</p> </li> <li> <p>Check the hardware requirements.</p> <p>Although the official minimum recommendation is:</p> <ul> <li>GeForce RTX 4080</li> <li>32GB RAM</li> <li>16GB VRAM</li> </ul> <p>You can still run Isaac Sim on lower configurations. For example:</p> <ul> <li>GeForce RTX 3070</li> <li>32GB RAM</li> <li>8GB VRAM</li> </ul> <p>This is the minimum workable configuration. It may run slower, but it is usable.</p> <p>If your computer cannot meet the minimum requirements, consider cloud deployment:</p> <p>Isaac Sim Cloud Deployment</p> </li> </ol>"},{"location":"setup/windows_isaac_sim/#download-isaac-sim-installation-package","title":"Download Isaac Sim Installation Package","text":"<ol> <li> <p>From the left menu, click Download Isaac Sim</p> </li> <li> <p>Select Windows</p> <p>Important Notes</p> <ul> <li> <p>Do NOT install Isaac Sim WebRTC Streaming Client, this is only for browser-based remote streaming from a server.</p> </li> <li> <p>Isaac Sim Assets: If your configuration is strong (RTX 4080, 32GB RAM, 16GB VRAM), you may download them as tutorial assets. For minimum configuration machines, I do not recommend    installing them.</p> </li> </ul> </li> <li> <p>The installation package size is approximately 7.3GB. Please be patient.</p> </li> </ol>"},{"location":"setup/windows_isaac_sim/#install-isaac-sim","title":"Install Isaac Sim","text":"<p>After downloading the installation package:</p> <ol> <li> <p>Create a folder (example):</p> <pre><code>C:\\isaac\n</code></pre> </li> <li> <p>Unzip the package into that folder and navigate into the folder and run:</p> <pre><code>post_install.bat\nisaac-sim.selector.bat\n</code></pre> </li> <li> <p>If installation is successful, you should see:</p> <pre><code>[140.262s] app ready\n[256.161s] Isaac Sim Full App is loaded.\n</code></pre> </li> </ol>"},{"location":"setup/windows_isaac_sim/#configure-ros-bridge-required","title":"Configure ROS Bridge (Required)","text":"<ol> <li>Double-click:        <pre><code>isaac-sim.selector.bat\n</code></pre></li> <li> <p>Add ROS Bridge Extension:</p> <pre><code>isaacsim.ros2.bridge\n</code></pre> </li> </ol> <p>This is required to connect Isaac Sim to a robot\u2019s ROS2 system running on Linux.</p> <p>(For detailed steps, see the ROS\u2013Isaac Connection Tutorial.)</p> <ol> <li>In the dropdown Use Internal ROS2 Libraries, select:        <pre><code>humble\n</code></pre></li> <li> <p>Enable only:</p> <ul> <li>Show startup console</li> <li>Enable eco mode on app startup</li> </ul> </li> </ol> <p>The reference picture is shown below:</p> <p></p> <p>After you set up, you will see the picture below, which demonstrates that you are successfully installed Isaac Sim.</p> <p></p> <ol> <li>Verify extension:<ul> <li>Click Window \u2192 Extensions</li> <li>Search ROS2 Bridge</li> <li>Enable it</li> </ul> </li> </ol> <p>The extension setting is shown in the picture below:</p> <p></p> <p>If everything is correct, Isaac Sim is successfully installed.</p>"}]}